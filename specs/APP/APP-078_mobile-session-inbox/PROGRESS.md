# PROGRESS · APP-078: Mobile Session Inbox

> Implementation Progress · current state, handoff notes, blockers, and verification status. This file is not a requirements source.

## Status

- **State**: in_progress
- **Branch**: `aarynlu/app-078-session-inbox`
- **Worktree**: `/Users/aarynlu/.cursor/worktrees/atmos/app078`
- **Last updated**: 2026-09-22
- **Current owner**: lead agent
- **Current phase**: review / PR

## Snapshot

- Specs are in this worktree, uncommitted, copied from the parent branch working tree.
- Wave 0 is running: backend catalog + WS contract (S0) and phone tab chrome (S1), file-disjoint.
- Session list UI (S2) and Web sidebar View (S3) wait until S0 lands.
- Do not edit this file from impl or review agents.

## Implementation Checklist

- [ ] Infra catalog table and repo
- [ ] Core service live-map merge and async catalog writes
- [ ] API WebSocket actions and api-types contract
- [ ] Mobile glass tabs and workspace header
- [ ] Mobile session inbox
- [ ] Web sidebar Session view
- [ ] Scenario tests (atmos-specs-test-run, after slices)

## Slice Kanban

> Orchestration board for `atmos-long-task-impl`. Not a requirements source.

| ID | Wave | Owns | Forbids | Depends | Status | Impl | Review | Verify |
|----|------|------|---------|---------|--------|------|--------|--------|
| S0 | 0 | infra catalog, agent_status service, WS action, api-types | `apps/mobile/**`, `apps/web/**` | — | in_progress | running | — | `cargo test -p core-service session_agent` |
| S1 | 0 | mobile tab host and workspace header | `crates/**`, `apps/api/**`, `packages/**`, `apps/web/**` | — | in_progress | running | — | `bun --filter @atmos/mobile typecheck` |
| S2 | 1 | `apps/mobile/src/features/sessions/**`, session routes | S0/S1 owns | S0, S1 | planned | — | — | `bun test apps/mobile/src/features/sessions` |
| S3 | 1 | web sidebar view + session grouping + en/zh keys | mobile and backend | S0 | planned | — | — | `bun test apps/web/src/app-shell/sidebar/session-grouping.test.ts` |

**Status**: `planned` · `ready` · `in_progress` · `blocked` · `in_review` · `rework` · `done`

## Slice Cards

### S0 — Catalog, snapshot, WS contract

- **Wave**: 0
- **Goal**: SQLite inbox catalog, per-session status list, archive action, api-types contract.
- **Out of scope**: phone UI, web UI, GitHub PR lookup.
- **Owns**: `crates/infra/src/db/**` for the new table only (migration, entity, repo, their `mod.rs` lines), `crates/core-service/src/service/agent_status/**`, `apps/api/src/api/ws/**` and `apps/api/src/main.rs` / `apps/api/src/app_state.rs` only for wiring the catalog, `packages/api-types/src/ws/**`, `packages/api-types/fixtures/actions.server.json`.
- **Forbids**: `apps/mobile/**`, `apps/web/**`, `PROGRESS.md`.
- **Depends**: —
- **Invariants**: see dispatch brief.
- **Verify**: `cargo test -p core-service session_agent && bun run --filter @atmos/api-types check-actions`
- **Review checklist**: two panes stay two rows; workspace rollup still omits Done; same bucket does not write; archive hides; restart display is Done.

### S1 — Glass tabs and workspace header

- **Wave**: 0
- **Goal**: Home is Workspace and Session glass tabs. Workspace keeps the current list. Disconnected Session does not sign in again.
- **Out of scope**: session rows, status fetch, PR, web.
- **Owns**: `apps/mobile/package.json`, `apps/mobile/bun.lock` if present else repo `bun.lock` only if the install touches it, `apps/mobile/app/**`, `apps/mobile/src/features/workspaces/WorkspaceListScreen.tsx`, `apps/mobile/src/ui/icons/lucide-native.ts` only to add tab icons.
- **Forbids**: `crates/**`, `apps/api/**`, `packages/**`, `apps/web/**`, `apps/mobile/src/features/sessions/**`.
- **Depends**: —
- **Verify**: `bun --filter @atmos/mobile typecheck`

## Decisions Since TECH

| ID | Decision | Why | Source update |
|----|----------|-----|---------------|
| D1 | Wave 0 splits backend contract from tab chrome | File-disjoint; session list needs the contract first | none |

## Verification Status

| Area | Command / Method | Last result | Notes |
|------|------------------|-------------|-------|
| Rust tests | `cargo test -p core-service session_agent` | not_run | S0 |
| api-types | `bun run --filter @atmos/api-types check-actions` | not_run | S0 |
| Mobile typecheck | `bun --filter @atmos/mobile typecheck` | not_run | S1 |

## Known Blockers

- [ ] None

## Handoff Notes

### Task goal

Ship APP-078 on `aarynlu/app-078-session-inbox` in `/Users/aarynlu/.cursor/worktrees/atmos/app078`.

### Current progress

Wave 0 dispatched: S0 backend, S1 mobile chrome.

### Constraints

Lead agent does not write feature code. Impl agents do not edit this file.

### Next steps

Review S0 and S1, then dispatch S2 and S3.

## Changed Areas

- Specs only, until wave 0 reports.
