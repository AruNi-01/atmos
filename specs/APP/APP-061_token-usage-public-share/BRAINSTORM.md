# Brainstorm · APP-061: Token Usage public share

> Problem space and exploration. Settled content graduates to PRD.md; committed architecture graduates to TECH.md.

## Context

Accounts, Hub, and device credentials already shipped in [APP-056](../APP-056_usage-share-and-accounts/PRD.md). Local Token Usage still only shares a **PNG card**. APP-056 designed multi-document `/s/{share_id}` pages on `atmos.land` plus auto-derived, renameable handles. Product now wants a **vanity page per user**: `https://atmos.land/tok/@handle`, one latest snapshot, manual refresh, social chips, and a public shell that matches the in-app charts.

Trigger: accounts are live; the remaining gap is a domain link people can post.

Current workaround: screenshot / PNG share / “open my laptop.”

## Goals (draft)

- Give a signed-in user one stable `atmos.land/tok/@handle` page.
- Keep local Token Usage login-free.
- Upload only the aggregates the public charts need.
- Reuse the in-app chart UI (same pixels), swap the fetch source.

## Options

### Option A — Multi-share documents (`/s/sh_…`) as in APP-056
Several snapshots per user; unlisted security via unguessable id.

**Pros**: Many named reports; private links do not leak a handle.  
**Cons**: Weak as a personal brand URL; extra list/manage UX.  
**Unknown**: Whether anyone needs more than one live page.

### Option B — One vanity page per user (chosen)
`/tok/@handle`, one latest snapshot, public or unlisted (`?k=`).

**Pros**: Fits X/GitHub bios; simple mental model; one D1 row.  
**Cons**: Handle is enumerable, so unlisted needs a secret; rename breaks links.  
**Unknown**: How often people want to change a handle (settled: never).

### Option C — Live multi-machine sync
Always-on ingest of local usage to Hub.

**Pros**: Page is always current.  
**Cons**: Privacy, merge rules, always-on dependency. Overkill for v1.

### Option D — New `packages/usage-share-view` or `packages/share`
Extract charts (and later session traces) into a shared package for landing + web.

**Pros**: Two apps can import one view.  
**Cons**: Speculative layer; session-share UI will not reuse these charts; APP-050 says extract on a second real consumer.  
**Unknown**: When a second share *type* actually ships.

### Option E — Render HTML inside `packages/hub`
Worker serves the public page.

**Pros**: One host.  
**Cons**: Hub is a control-plane Worker, not a Next app; would fork the chart stack.

## Key forks in the road

| Fork | Decision |
|------|----------|
| Host URL | **`https://atmos.land/tok/@handle`** (not `hub.atmos.land`) |
| Cardinality | **One live page per user**; overwrite on manual refresh |
| Update | **Manual only** (“Update page”) |
| Handle | User-chosen; **claim once**; never rename |
| Unlisted URL | May include the handle; gate with secret `k` |
| Chart UI | Extract **inside `apps/web`**; no new package |
| Public HTML owner | **`apps/web` `/tok/[handle]`**; Cloudflare rewrite keeps `atmos.land` |
| Storage | Slim snapshot: daily totals + top 5 ranks + `other`; not raw tokscale |
| Future share types | Same **host/shell** later; not a kitchen-sink share package now |

## Open questions

- [x] atmos.land vs hub.atmos.land → atmos.land
- [x] One page vs many shares → one
- [x] Auto-push vs manual → manual
- [x] Handle mutability → once
- [x] New view package → no
- [x] Avatar click target → **not a link** (social chips on the right are the links)
- [ ] Exact Cloudflare rewrite vs 302 fallback if asset prefix is painful — decide in TECH, keep user-visible URL `atmos.land`

## References

- [APP-056](../APP-056_usage-share-and-accounts/TECH.md) — Hub, Better Auth, `usage_shares`, `user_profiles`, redaction
- [APP-050](../APP-050_shared-package-layering/TECH.md) — package boundaries
- Code: `apps/web/src/app-shell/TokenUsagePage.tsx`, `TokenUsageOverviewTab.tsx`, `token-usage-dialog-utils.ts`, `packages/hub/src/usage-shares.ts`, `packages/hub/src/db/schema.ts`

## Ready to promote

- Promote to PRD: vanity URL, one snapshot, claim-once handle, public vs unlisted, social chips, Generated by Atmos, local-first.
- Promote to TECH: slim payload (top 5 + other), Hub profile+snapshot, web-only view extract, atmos.land rewrite, no new packages.
