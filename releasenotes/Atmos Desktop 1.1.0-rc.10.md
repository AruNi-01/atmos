# Atmos Desktop 1.1.0-rc.10

## Changes Since 1.1.0-rc.9

### Atmos Computer (APP-016)

- Relay HTTP gateway for remote Computer API access (CLI / REST alignment with UI)
- `client-session.json` for relay target discovery (replaces legacy `local/state.json`)
- Unified local runtime manifest; relay outbound ingest improvements

### Other

- Prerelease build from branch `aarynlu/app-016-atmos-computer` for integration testing.

---

_Pre-release — not for production use._
