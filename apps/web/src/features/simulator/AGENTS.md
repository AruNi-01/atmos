# Workspace Simulator feature

The panel owns sidebar and center surfaces.
The session store is filled only by `simulator://` events.
Keep browser rendering hosted-only; do not clone a browser session here.
Keep stream protocol helpers local to this feature.
