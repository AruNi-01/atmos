---
title: Cross-Platform & Remote Access
description: Use Atmos locally, or share this computer through a browser link.
---

Atmos can run as a desktop app or in a browser. Remote Access is for sharing **this computer's** Atmos session through a browser link.

This is different from [Atmos Computer](/features/atmos-computer), where the app connects to another machine where the work runs.

## Two different “remote” mechanisms

| | **Remote Access** (this page) | **Atmos Computer** |
|---|------------------------------|---------------------|
| **Goal** | Share **this** running Atmos session | Work on **another** machine |
| **Connection** | Tailscale, Cloudflare Tunnel, or ngrok | Atmos Computer |
| **Where work runs** | The machine you are sitting at | The machine you registered |
| **Typical client** | Browser opens **Public URL** | Desktop/Web **Connect** in **My Computers** |
| **Where to configure** | **Desktop app only** → Settings → **Remote Access** | Settings → **Atmos Computer** |
| **Tunnel apps on your laptop** | Yes (provider tools) | **No** |

Do not mix the two: turning on **Remote Access** does not register an Atmos Computer, and **Connect** to an Atmos Computer does not start Tailscale or ngrok.

## Cross-platform clients (local)

Use Atmos on the machine where your work runs:

| Client | How you use it |
|--------|----------------|
| **Desktop app** | Native app; talks to Atmos on the same machine automatically |
| **Web (local)** | Browser at your local address (e.g. after installing the local web runtime) |
| **Mobile** | Planned — follow release notes |

This is normal local use: no sharing link or remote computer is involved.

## Remote Access (integrated tunneling)

**Remote Access** lets you publish **this computer’s** Atmos so you—or someone else—can open it in a **web browser** via a **Public URL**.

### How It Works

1. Atmos prepares a shareable browser entry for this machine.
2. You choose a provider: **Tailscale**, **Cloudflare Tunnel**, or **ngrok**.
3. The desktop app helps you install or sign in to that provider, then starts the tunnel.
4. Atmos shows a **Public URL** (and optional share link or entry code) in **Settings → Remote Access** and in the header tunnel menu.
5. Visitors open that URL in a browser and work with the Atmos session you shared.

Your day-to-day Atmos data still stays on this machine; the tunnel only provides a controlled front door for browsers.

### Where to configure

1. Use the **Desktop app** (**Remote Access** is not available in browser-only local web mode).
2. Open **Settings** → **Remote Access**.
3. **Detect** providers; follow install or sign-in steps (the app can open a helper terminal).
4. Choose **private** or **public** mode where offered, set how long the link stays valid, then **Start**.
5. Copy **Public URL** and open it on another device (your LAN, Tailscale network, or the public internet—depending on the provider).

### Provider notes

- **Tailscale** — virtual private network; good when your devices already share a tailnet.
- **Cloudflare Tunnel** — HTTPS through Cloudflare; no need to open a port on your router.
- **ngrok** — quick public HTTPS link for demos and sharing.

You can run one active tunnel per provider type. The header shows running tunnels and reminders before a link expires.

### Stop sharing

**Stop** the tunnel in Settings or the header menu. Links can also expire automatically; renew or recover from the same screen when needed.

## When to use which

| Scenario | Use |
|----------|-----|
| “I’m on my dev laptop; open Atmos from my phone browser **on this machine**” | **Remote Access** |
| “My code and agents should run on a remote machine; my Mac only runs the UI” | **Atmos Computer** → [Remote Build](/workflows/remote-build) |
| “I already use Tailscale on this box” | **Remote Access** (Tailscale) |
| “No tunnel apps—I want the UI to connect to another machine” | **Atmos Computer** |

## Related

- [Atmos Computer](/features/atmos-computer) — remote machines without a tunnel
- [Remote Build](/workflows/remote-build) — register and connect workflow
