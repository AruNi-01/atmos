# direnv: load the reproducible Nix dev shell automatically on `cd`.
# Requires direnv (https://direnv.net). Run `direnv allow` once to enable.
# nix-direnv is recommended for caching: https://github.com/nix-community/nix-direnv
use flake
watch_file rust-toolchain.toml
watch_file flake.lock
