# `@atmos/md-live` (APP-067)

UI-free codec: `:md-live` / `::md-live` directives, `<!--atmos-md-live-->` fence extractor, AgentRequest prompt rendering, Canvas-style `Untitled.md` names.

**Must not** import `api-*`, `apps/*`, `@workspace/ui`, or `@atmos/shared`. Host UI lives in `apps/web/src/features/md-live`.
