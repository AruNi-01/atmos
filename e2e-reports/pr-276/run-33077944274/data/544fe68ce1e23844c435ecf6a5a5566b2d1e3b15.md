# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: smoke/workspace/workspace-sidebar-routes.e2e.ts >> smoke workspace >> @smoke @stateful exercises workspace center tool tabs and read-only subtabs
- Location: tests/smoke/workspace/workspace-sidebar-routes.e2e.ts:16:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('tab', { name: /^(评审|Review)$/ })
Expected: visible
Timeout: 45000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 45000ms
  - waiting for getByRole('tab', { name: /^(评审|Review)$/ })

```

```yaml
- alert
- region "Notifications"
- region "Notifications"
- region "Notifications"
- banner:
  - button "收起左侧边栏"
  - button "返回上一步"
  - button "前进"
  - button "刷新页面"
  - button "打开 O"
  - button
  - status "无法将远程分支与 origin/main 进行比较"
  - button "atmos/vileplume"
  - button "origin/ main"
  - button "搜索": 搜索... K
  - button "打开工作区摘要"
  - button "用量"
- complementary:
  - button "Launchpad":
    - img
    - text: Launchpad
  - button "工作区":
    - img
  - button "磁盘分析器":
    - img
  - navigation "Launchpad":
    - button "技能":
      - img
      - text: 技能
    - button "自动化":
      - img
      - text: 自动化
    - button "Token 用量":
      - img
      - text: Token 用量
    - button "画布"
    - button "Prototype Design":
      - img
      - text: Prototype Design
    - button "任务":
      - img
      - text: 任务
    - button "新建工作区":
      - img
      - text: 新建工作区
  - button "Atmos E2E"
  - button
  - button
  - button "vileplume"
  - button "添加项目"
  - button
  - button "打开设置"
- separator
- main:
  - tablist:
    - button "关闭 1 1":
      - tab "关闭 1 1":
        - button "关闭 1"
        - text: "1"
    - button "图形历史":
      - tab "图形历史" [selected]:
        - button "关闭图形历史标签页"
        - text: 图形历史
    - button "新建标签页"
    - button "打开标签组"
  - group:
    - textbox "搜索提交…"
    - button "区分大小写"
  - button "上一个匹配" [disabled]
  - button "下一个匹配" [disabled]
  - button "刷新"
  - separator "调整图形列宽"
  - separator "调整描述列宽"
  - separator "调整日期列宽"
  - separator "调整作者列宽"
  - text: 图形 描述 日期 作者 提交
  - button "pull/276/merge Merge 07a9b2036c799f2ee15020de90ca65ddd9905d90 into 2c74659b8272a3dce7285fb4ac13edc1e23ec99d 27 8月 2026 13:39 AruNi_Lu b6ce040 复制提交哈希":
    - text: pull/276/merge Merge 07a9b2036c799f2ee15020de90ca65ddd9905d90 into 2c74659b8272a3dce7285fb4ac13edc1e23ec99d 27 8月 2026 13:39 AruNi_Lu b6ce040
    - button "复制提交哈希"
  - 'button "atmos/vileplume origin/main fix(web): stop first-file discard from clearing the changes list 27 8月 2026 12:37 AarynLu 2c74659 复制提交哈希"':
    - text: "atmos/vileplume origin/main fix(web): stop first-file discard from clearing the changes list 27 8月 2026 12:37 AarynLu 2c74659"
    - button "复制提交哈希"
- contentinfo:
  - 'button "资源监视器: CPU 28% · 内存 12%"': 监视器
  - button "本地 0"
  - button "休眠中 ~"
```

# Test source

```ts
  1   | import { expect, test } from "../../../fixtures/test";
  2   | import {
  3   |   buildProjectWorkspaceDeepLink,
  4   |   closeSettingsPage,
  5   |   connectLocalComputer,
  6   |   expectHealthyRoute,
  7   |   getCenterStage,
  8   |   gotoContextRoute,
  9   |   gotoSettingsRoute,
  10  |   normalizePathname,
  11  |   stubComputerClientSettingsApi,
  12  |   withSearchParams,
  13  | } from "../support/app-smoke";
  14  | 
  15  | test.describe("smoke workspace", () => {
  16  |   test("@smoke @stateful exercises workspace center tool tabs and read-only subtabs", async ({
  17  |     page,
  18  |   }) => {
  19  |     await stubComputerClientSettingsApi(page);
  20  |     await connectLocalComputer(page, { locale: "zh" });
  21  | 
  22  |     await expectHealthyRoute(page, "/", { locale: "zh" });
  23  |     await expect(page.getByRole("button", { name: /搜索|Search/ })).toBeVisible({
  24  |       timeout: 45_000,
  25  |     });
  26  | 
  27  |     const contextUrl = withSearchParams(await buildProjectWorkspaceDeepLink(page), {
  28  |       activeSettingTab: null,
  29  |     });
  30  |     // Tool-tab `?tab=` on the project wrapper is leftover chrome after the
  31  |     // workspace preview hop and gets stripped; later tool tabs must target
  32  |     // the workspace URL itself.
  33  |     const workspaceUrl = new URL(contextUrl).searchParams.get("pvUrl");
  34  |     expect(workspaceUrl, "missing workspace url in project deep link").toBeTruthy();
  35  | 
  36  |     await gotoContextRoute(page, withSearchParams(contextUrl, { tab: "files" }), {
  37  |       locale: "zh",
  38  |     });
  39  |     await expect(page.getByRole("tab", { name: /^(文件|Files)$/ })).toBeVisible({
  40  |       timeout: 45_000,
  41  |     });
  42  |     await expect
  43  |       .poll(async () => new URL(page.url()).searchParams.get("tab"))
  44  |       .toBeNull();
  45  | 
  46  |     await gotoContextRoute(page, withSearchParams(workspaceUrl!, { tab: "changes" }), {
  47  |       locale: "zh",
  48  |     });
  49  |     await expect(page.getByRole("tab", { name: /^(变更|Changes)$/ })).toBeVisible({
  50  |       timeout: 45_000,
  51  |     });
  52  |     const changesStage = await getCenterStage(page);
  53  |     const scopeTrigger = changesStage.getByRole("button", {
  54  |       name: /选择变更范围|Select changes scope/,
  55  |     });
  56  |     await expect(scopeTrigger).toBeVisible({ timeout: 45_000 });
  57  |     await scopeTrigger.click();
  58  |     await page.getByRole("menuitem", { name: /^(图形历史|Graph History)$/ }).click();
  59  |     // The tab's computed name can include the close control; do not require an exact match.
  60  |     await expect(page.getByRole("tab", { name: /图形历史|Graph History/ })).toBeVisible();
  61  | 
  62  |     await gotoContextRoute(page, withSearchParams(workspaceUrl!, { tab: "review" }), {
  63  |       locale: "zh",
  64  |     });
> 65  |     await expect(page.getByRole("tab", { name: /^(评审|Review)$/ })).toBeVisible({
      |                                                                    ^ Error: expect(locator).toBeVisible() failed
  66  |       timeout: 45_000,
  67  |     });
  68  | 
  69  |     await gotoContextRoute(page, withSearchParams(workspaceUrl!, { tab: "run" }), {
  70  |       locale: "zh",
  71  |     });
  72  |     const runStage = await getCenterStage(page);
  73  |     // The Run surface's inner terminal strip reuses the same 运行/Run tab name.
  74  |     await expect(
  75  |       runStage.getByRole("tablist").first().getByRole("tab", { name: /运行|Run/ }),
  76  |     ).toBeVisible();
  77  | 
  78  |     await gotoContextRoute(page, withSearchParams(workspaceUrl!, { tab: "github" }), {
  79  |       locale: "zh",
  80  |     });
  81  |     await expect(page.getByRole("tab", { name: /^GitHub$/ })).toBeVisible({
  82  |       timeout: 45_000,
  83  |     });
  84  |     const githubStage = await getCenterStage(page);
  85  |     await expect(githubStage.getByRole("tab", { name: "拉取请求" })).toBeVisible();
  86  |     await expect(githubStage.getByRole("tab", { name: "议题" })).toBeVisible();
  87  |     await expect(githubStage.getByRole("tab", { name: "操作" })).toBeVisible();
  88  |   });
  89  | 
  90  |   test("@smoke @stateful boots direct workspace urls with read-only sidebar toggles and settings page", async ({
  91  |     page,
  92  |   }) => {
  93  |     await stubComputerClientSettingsApi(page);
  94  |     await connectLocalComputer(page, { locale: "zh" });
  95  | 
  96  |     const contextUrl = await buildProjectWorkspaceDeepLink(page);
  97  |     const workspaceUrl = new URL(contextUrl).searchParams.get("pvUrl");
  98  |     expect(workspaceUrl, "missing workspace url in project deep link").toBeTruthy();
  99  | 
  100 |     const projectsRoute = withSearchParams(workspaceUrl!, {
  101 |       lsTab: "projects",
  102 |       lsTask: "true",
  103 |       settingsModal: null,
  104 |       activeSettingTab: null,
  105 |     });
  106 |     const firstResponse = await page.goto(projectsRoute, {
  107 |       waitUntil: "domcontentloaded",
  108 |     });
  109 |     expect(firstResponse, `missing navigation response for ${projectsRoute}`).not.toBeNull();
  110 |     expect(firstResponse!.status(), `unexpected status for ${projectsRoute}`).toBeLessThan(500);
  111 |     await expect
  112 |       .poll(async () => normalizePathname(new URL(page.url()).pathname))
  113 |       .toBe("/workspace");
  114 |     await expect
  115 |       .poll(async () => new URL(page.url()).searchParams.get("id") ?? "")
  116 |       .not.toBe("");
  117 |     await expect
  118 |       .poll(async () => new URL(page.url()).searchParams.get("lsTab"))
  119 |       .toBe("projects");
  120 |     await expect
  121 |       .poll(async () => new URL(page.url()).searchParams.get("lsTask"))
  122 |       .toBe("true");
  123 |     await expect
  124 |       .poll(async () => page.locator("html").getAttribute("lang"))
  125 |       .toBe("zh");
  126 |     await expect(page.locator("body")).toBeVisible();
  127 | 
  128 |     const filesRoute = withSearchParams(workspaceUrl!, {
  129 |       tab: "files",
  130 |       lsTask: null,
  131 |       settingsModal: null,
  132 |       activeSettingTab: null,
  133 |     });
  134 |     const secondResponse = await page.goto(filesRoute, {
  135 |       waitUntil: "domcontentloaded",
  136 |     });
  137 |     expect(secondResponse, `missing navigation response for ${filesRoute}`).not.toBeNull();
  138 |     expect(secondResponse!.status(), `unexpected status for ${filesRoute}`).toBeLessThan(500);
  139 |     await expect(page.getByRole("tab", { name: /^(文件|Files)$/ })).toBeVisible({
  140 |       timeout: 45_000,
  141 |     });
  142 |     await expect
  143 |       .poll(async () => new URL(page.url()).searchParams.get("lsTask") ?? "")
  144 |       .toBe("");
  145 | 
  146 |     await gotoSettingsRoute(page, "keyboard", { locale: "zh" });
  147 |     await expect
  148 |       .poll(async () => normalizePathname(new URL(page.url()).pathname))
  149 |       .toBe("/settings");
  150 |     await expect
  151 |       .poll(async () => new URL(page.url()).searchParams.get("activeSettingTab"))
  152 |       .toBe("keyboard");
  153 |     await expect(page.getByRole("button", { name: /^(Keyboard|键盘)$/ })).toBeVisible();
  154 |     await expect(page.getByRole("dialog", { name: /^(Settings|设置)$/ })).toHaveCount(0);
  155 | 
  156 |     await closeSettingsPage(page);
  157 |     await expect
  158 |       .poll(async () => normalizePathname(new URL(page.url()).pathname))
  159 |       .not.toBe("/settings");
  160 |   });
  161 | });
  162 | 
```