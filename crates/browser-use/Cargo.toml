[package]
name = "browser-use"
version = "0.1.0"
edition = "2021"
description = "Atmos Browser Use — page CDP control (separate from Desktop Use)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
desktop-use = { path = "../desktop-use" }

[dev-dependencies]
tempfile = "3.10"
