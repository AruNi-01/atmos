[package]
name = "permission-access"
version = "0.1.0"
edition = "2021"
description = "Atmos Permission Access: local product presence and user consent before touching sensitive host data."

[dependencies]
dirs = "6.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
